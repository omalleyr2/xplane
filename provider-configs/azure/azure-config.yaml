apiVersion: azure.upbound.io/v1beta1
metadata:
  name: newConfig
kind: ProviderConfig
spec:
  credentials:
    source: Secret
    secretRef:
      namespace: crossplane-system
      name: azure-secret
      key: creds
